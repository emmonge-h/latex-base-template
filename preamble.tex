% Encoding of input files in UTF-8
\usepackage[utf8]{inputenc}

% Language and typographical rules for English
\usepackage[english]{babel}

% Citations, URL's, and in-document references
\usepackage{cite}
\usepackage[colorlinks=true, 
            linkcolor = blue,
            urlcolor  = blue,
            citecolor = blue,
            anchorcolor = blue]{hyperref}

% Math-related symbols and fonts
\usepackage{amsmath,amssymb,amsfonts,amsthm,cancel}

% For writing pseudocode and algorithms
\usepackage{algorithmic}

% Including and manipulating graphics
\usepackage{graphicx}

% Special symbols like the degree symbol, etc.
\usepackage{textcomp}

% Color support for table cells and other environments
\usepackage[table]{xcolor}

% Force the placement of floats like figures or tables
\usepackage{float}

% Advanced list customization
\usepackage{enumitem}

% Drop cap for the first letter of a paragraph
\usepackage{lettrine}

% Advanced array and table formatting
\usepackage{array}

% Customization of captions for figures and tables
\usepackage{caption}

% For large comment blocks
\usepackage{comment}

% Support for multiple columns
\usepackage{multicol}

% Support for rows spanning multiple rows in tables
\usepackage{multirow}

% Include entries like bibliography and index in the table of contents
\usepackage{tocbibind}

% Dummy text
\usepackage{lipsum}

% Customization of section and chapter titles
\usepackage{titlesec}

% Support for tables spanning multiple pages
\usepackage{longtable}

% If work requires APA
% \usepackage{apacite}

% Listings to add code
\usepackage{listings}
\lstset
{
    language=C,
    basicstyle=\footnotesize,
    numbers=left,
    stepnumber=1,
    showstringspaces=false,
    tabsize=1,
    breaklines=true,
    breakatwhitespace=false,
}
